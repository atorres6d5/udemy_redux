//state arrg is not app state only
//the satate this reducer is resposable for


export default function(state=null, action){
switch (action.type) {
  case 'BOOK_SELECTED': return action.payload
    }
return state
}
